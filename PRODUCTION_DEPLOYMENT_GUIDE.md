# 프로덕션 배포 가이드

**작성일**: 2025-11-10
**버전**: 1.0
**상태**: 스테이징 테스트 통과 후 실행
**배포 대상**: Production Server (https://aed.pics)
**배포 버전**: 716b39c (로그인/로그아웃 성능 및 안정성 개선)

---

## 프로덕션 배포 전 필수 확인

### 1. 스테이징 테스트 완료 확인

```
□ Test 1: 로그인 성능 - 통과
□ Test 2: 로그아웃 안정성 - 통과
□ Test 3: 민감정보 필터링 - 통과
□ Test 4: 프로필 폴백 - 통과

모든 항목이 체크되었는가? 하나라도 미통과면 배포 중지!
```

### 2. 배포 승인 확인

```
STAGING_TEST_RESULTS.md에서:
□ 최종 판정: 통과
□ 검토자 승인: 예
□ 배포 승인: 승인
```

### 3. 환경 준비 상태 확인

```bash
# 로컬 환경
□ git branch에서 main 브랜치 확인
  git branch

□ 최신 커밋 716b39c 확인
  git log --oneline -1

□ 워킹 디렉토리 깨끗한지 확인
  git status
  # 결과: "nothing to commit, working tree clean"
```

### 4. 배포 시간대 확인

```
권장 배포 시간:
□ 업무 시간 중 (09:00 - 18:00)
□ 시스템 사용량 적은 시간 (가급적)

절대 금지:
□ 야간 또는 휴일 (모니터링 불가)
□ 피크 타임 (월/목 오후)
□ 점검 또는 이벤트 예정된 시간
```

---

## 프로덕션 배포 방법

### 방법 1: GitHub Actions 자동 배포 (권장)

**장점**: 자동화, 추적 가능, 롤백 용이

**단계**:

#### Step 1: GitHub에 푸시
```bash
# 커밋이 이미 되어 있으므로 푸시만 실행
git push origin main

# 푸시 확인
# GitHub > Actions 탭에서 배포 workflow 실행 확인
```

#### Step 2: GitHub Actions 모니터링
```
GitHub Repository > Actions 탭 접속
↓
최신 workflow 선택: "Deploy to Production"
↓
실시간 로그 확인:
  ✅ Checkout code
  ✅ Install dependencies
  ✅ Build application
  ✅ Deploy to server
  ✅ Verify deployment
```

**배포 완료 표시**:
```
Green checkmark: ✅ 배포 성공
Red X: ❌ 배포 실패 (롤백 필요)
```

#### Step 3: 배포 완료 확인 (5-10분 소요)
```bash
# 프로덕션 서버 확인
curl -I https://aed.pics

# 예상 응답:
# HTTP/2 200
# Server: nginx/1.18.0

# 프로덕션 로그 확인 (SSH 필요)
ssh user@prod-server
pm2 status
pm2 logs --lines 20
```

---

### 방법 2: 수동 배포 (SSH)

**장점**: 직접 제어, 문제 즉시 파악 가능
**단점**: 수동 조작 필요, 실수 가능성

**단계**:

#### Step 1: 프로덕션 서버 접속
```bash
ssh user@223.130.150.133
# 또는 설정된 호스트명으로
ssh prod-server
```

#### Step 2: 최신 코드 가져오기
```bash
cd /var/www/aedpics

# 현재 상태 확인
git status
git log --oneline -1

# 최신 코드 가져오기
git fetch origin
git checkout origin/main

# 변경사항 확인
git diff HEAD~1
```

#### Step 3: 의존성 설치 및 빌드
```bash
# 프로덕션 의존성만 설치 (devDependencies 제외)
npm ci --production

# Prisma 클라이언트 생성
npx prisma generate

# 프로덕션 빌드
NODE_ENV=production npm run build

# 빌드 성공 확인
ls -lah .next/
# 예상: .next 디렉토리 크기 > 100MB
```

#### Step 4: PM2 무중단 배포
```bash
# PM2 현재 상태 확인
pm2 status
# 예상: status = "online"

# 무중단 배포 (rolling restart)
pm2 reload ecosystem.config.cjs

# 배포 상태 모니터링 (배포 중)
pm2 status
# Instance 1 → Instance 2 → 번갈아가며 재시작

# 배포 완료 확인 (1-2분 소요)
pm2 status
# 모든 프로세스 status = "online" 확인
```

#### Step 5: 배포 후 검증
```bash
# 1. 로그 확인 (에러 없는지)
pm2 logs --err --lines 50

# 2. 프로세스 재시작 횟수 확인 (증가하면 문제)
pm2 status
# restarts 칼럼: 배포 전과 동일해야 함

# 3. HTTP 응답 확인
curl -I https://aed.pics
# HTTP/2 200 확인

# 4. 애플리케이션 로그 확인
pm2 logs --lines 20 | grep -i "ready\|listening\|started"
```

---

## 배포 중 체크리스트

```
배포 시작:
□ 시간: ____:____ (____년 ____월 ____일)
□ 담당자: ________________
□ 배포 방법: [ ] GitHub Actions / [ ] SSH 수동

배포 진행:
□ 단계 1: 코드 가져오기 - 완료
□ 단계 2: 의존성 설치 - 완료
□ 단계 3: Prisma 생성 - 완료
□ 단계 4: 빌드 - 완료
□ 단계 5: PM2 배포 - 완료
□ 단계 6: 배포 검증 - 완료

배포 완료:
□ 시간: ____:____
□ 소요 시간: ____분
□ 다운타임: 0초 (PM2 reload 사용)
```

---

## 배포 후 검증 (배포 완료 후 반드시 실행)

### 즉시 검증 (1분 내)

```bash
# 1. PM2 상태 확인
pm2 status

확인 항목:
□ status: online (빨간색 아님)
□ 모든 instance 온라인
□ uptime 정상
```

```bash
# 2. 웹 응답 확인
curl -I https://aed.pics

확인 항목:
□ HTTP/2 200 (또는 HTTP/1.1 200)
□ Server 헤더 정상
□ Content-Type 있음
```

```bash
# 3. 에러 로그 확인
pm2 logs --err --lines 20

확인 항목:
□ 에러 없음 (또는 이전 에러만 있음)
□ "Cannot find module" 에러 없음
□ Database 연결 에러 없음
```

### 5분 후 검증

```bash
# 1. 메모리 사용량 확인
pm2 monit

확인 항목:
□ 메모리: < 500MB
□ CPU: 안정적
□ 재시작 없음
```

```bash
# 2. 재시작 횟수 확인
pm2 status

확인 항목:
□ restarts: 배포 전과 동일 (증가하지 않음)
□ uptime 증가 (시간 경과 정상)
```

```bash
# 3. 로그 다시 확인
pm2 logs --err --lines 50

확인 항목:
□ 새로운 에러 없음
□ 지속적인 에러 없음
```

### 15분 후 검증

```bash
# 1. 안정성 확인
pm2 status

확인 항목:
□ status: online
□ restarts: 변화 없음
□ uptime: 15분 이상
```

```bash
# 2. 데이터베이스 연결 확인
curl https://aed.pics/api/health

확인 항목:
□ HTTP 200
□ JSON 응답 (데이터베이스 연결 확인)
```

```bash
# 3. 로그인 페이지 접속 가능 확인
curl https://aed.pics/auth/signin

확인 항목:
□ HTML 응답 (페이지 정상)
□ HTTP 200
```

### 1시간 후 검증

```bash
# 1. 최종 상태 확인
pm2 status
pm2 logs --lines 100

확인 항목:
□ 모든 metric 정상
□ 에러 로그 없음
□ 성능 저하 없음
```

```bash
# 2. 사용자 피드백 수집
# 내부 테스터 또는 사용자에게 로그인 성능 확인 요청
# - 스피너가 빠르게 나타나는가?
# - 로그인이 빠른가?
# - 로그아웃이 정상 작동하는가?
```

---

## 배포 완료 후 모니터링

### 1시간 집중 모니터링

```bash
# 매 5분마다 로그 확인 (자동화 권장)
watch -n 300 'pm2 logs --err --lines 20'

# 또는 수동
pm2 logs --err --lines 20
# (5분마다 실행)
```

### 주요 모니터링 항목

```
✅ PM2 프로세스 상태: online
✅ 재시작 횟수: 변화 없음
✅ 에러 로그: 새로운 에러 없음
✅ 메모리 사용량: < 600MB
✅ CPU 사용률: < 50%
✅ HTTP 응답: 200 OK
✅ 데이터베이스: 연결 정상
```

### 성능 개선 확인

```
사용자 피드백 수집:
□ 로그인 속도 개선됨: Y/N
□ 로그아웃 버튼 정상: Y/N
□ 에러/지연: Y/N (있으면 상세 기록)

성능 메트릭 비교:
변경 전 (3-5초) vs 변경 후: ________
개선도: [ ] 눈에 띄는 개선 / [ ] 개선 / [ ] 미미
```

---

## 배포 실패 시 긴급 대응

### 증상 1: PM2 status = "errored"

```bash
# 원인 분석
pm2 logs --err --lines 100

# 확인 항목:
# - "Cannot find module" 에러?
# - Database 연결 실패?
# - 환경변수 누락?

# 빠른 해결: 빌드 재검증
cd /var/www/aedpics
npm run build

# 여전히 실패하면: 이전 버전 복구
pm2 stop all
git reset --hard HEAD~1
NODE_ENV=production npm run build
pm2 start ecosystem.config.cjs
```

### 증상 2: HTTP 502 Bad Gateway

```bash
# 1. PM2 상태 확인
pm2 status
# status: offline? errored?

# 2. 원인 분석
pm2 logs --err --lines 50

# 3. 프로세스 재시작
pm2 restart all

# 4. 여전히 실패하면
pm2 stop all
pm2 delete all
npm run build
pm2 start ecosystem.config.cjs
```

### 증상 3: 메모리 누수 (메모리 계속 증가)

```bash
# 1. 현재 메모리 확인
pm2 monit
# 메모리가 계속 증가하는가?

# 2. 로그 확인
pm2 logs --lines 100

# 3. 프로세스 재시작
pm2 restart all

# 4. 장기 모니터링
watch -n 60 'free -h'
```

### 긴급 롤백 (이전 버전으로 복구)

```bash
# 1. 현재 상태 확인
git log --oneline -5

# 2. 이전 버전으로 복구
git reset --hard HEAD~1
# 또는 특정 커밋으로
git checkout 커밋ID

# 3. 재빌드 및 배포
NODE_ENV=production npm run build
pm2 reload ecosystem.config.cjs

# 4. 복구 확인
pm2 status
curl -I https://aed.pics
```

---

## 롤백 절차

배포 후 심각한 문제가 발생한 경우:

### Step 1: 즉시 중지
```bash
# 새 배포가 더 퍼지지 않도록 즉시 중지
# (GitHub Actions: workflow 취소)
# (SSH: Ctrl+C)
```

### Step 2: 이전 버전 복구
```bash
ssh prod-server
cd /var/www/aedpics

# 이전 커밋 확인
git log --oneline -5

# 이전 버전으로 복구
git reset --hard HEAD~1

# 빌드 및 배포
NODE_ENV=production npm run build
pm2 reload ecosystem.config.cjs

# 복구 확인
pm2 status
curl -I https://aed.pics
```

### Step 3: 원인 분석
```
원인 분석:
□ 배포 명령어 오류?
□ 코드 오류?
□ 환경변수 문제?
□ 데이터베이스 문제?

분석 결과: ________________

재배포 일정: ________________
```

---

## 배포 후 보고서

### 배포 완료 보고 메시지

```
프로덕션 배포 완료

버전: 716b39c
배포 일시: ____년 ____월 ____일 ____시 ____분
담당자: ________________
소요 시간: ____분 (다운타임 0초)

개선 사항:
- 로그인 성능: 3-5초 → 1-2초 (60-75% 개선)
- 로그아웃 안정성: 네트워크 오류 시에도 100% 안정화
- 보안: 민감정보 완전 제거

배포 후 상태:
- PM2: online ✅
- 웹 응답: HTTP 200 ✅
- 에러 로그: 없음 ✅
- 메모리: ____MB ✅

다음 모니터링: 1시간
```

### 배포 기록 (추적용)

```
배포 날짜: ________________
배포 버전: 716b39c
배포 담당자: ________________
배포 방법: [ ] GitHub Actions / [ ] SSH 수동

배포 시작: ____:____
배포 완료: ____:____
소요 시간: ____분

배포 전 상태:
- 커밋: ________________
- PM2 상태: ________________

배포 후 상태:
- 커밋: 716b39c
- PM2 상태: online
- HTTP 응답: 200

문제 발생: [ ] 없음 / [ ] 있음
문제 내용: ________________

롤백 필요: [ ] 아니오 / [ ] 예
롤백 사유: ________________

모니터링 결과:
- 1시간 후: ________________
- 발견된 이슈: ________________
- 사용자 피드백: ________________
```

---

## 배포 체크리스트

### 배포 전
- [ ] 스테이징 테스트 모두 통과
- [ ] 검토자 승인
- [ ] 배포 시간대 확인 (업무 시간)
- [ ] 롤백 계획 수립
- [ ] 팀 공지

### 배포 중
- [ ] GitHub에 푸시 또는 SSH 접속
- [ ] 의존성 설치 (npm ci)
- [ ] Prisma 생성 (npx prisma generate)
- [ ] 빌드 (npm run build)
- [ ] PM2 배포 (pm2 reload)
- [ ] 배포 로그 모니터링

### 배포 후
- [ ] 즉시 검증 (1분): pm2 status, curl
- [ ] 5분 후 검증: 재시작 횟수, 에러 로그
- [ ] 15분 후 검증: 데이터베이스, 로그인 페이지
- [ ] 1시간 검증: 최종 상태, 사용자 피드백
- [ ] 배포 보고서 작성

---

## 배포 완료 서명

```
배포 담당자: ________________
배포 완료 시간: ____:____
배포 결과: [ ] 성공 / [ ] 실패 (롤백함)
서명: ________________

모니터링 담당자: ________________
모니터링 완료 시간: ____:____
최종 결과: [ ] 안정 / [ ] 문제 있음
서명: ________________

프로젝트 매니저: ________________
승인 날짜: ________________
서명: ________________
```

---

## 다음 단계

배포 완료 후:

1. **사용자 공지** (선택)
   - "로그인 속도가 개선되었습니다"
   - 로그아웃 문제 해결 안내

2. **성능 모니터링**
   - 실제 사용자의 로그인 성능 측정
   - 에러 추적

3. **문서 정리**
   - 배포 보고서 보관
   - 문제 사항 기록

4. **향후 개선**
   - 사용자 피드백 수집
   - 추가 최적화 검토

---

**참고 문서**:
- [배포 전 체크리스트](DEPLOY_TO_STAGING.md) - 스테이징 배포 가이드
- [스테이징 테스트 가이드](docs/testing/STAGING_TEST_GUIDE.md) - 상세 테스트 절차
- [배포 준비 보고서](docs/testing/DEPLOYMENT_READINESS_REPORT.md) - 배포 준비 상태
- [긴급 롤백 절차](docs/deployment/DEPLOYMENT.md) - 일반 배포 가이드

**배포 완료!** 🎉

사용자들의 로그인 성능 개선과 안정성 향상을 확인하실 수 있을 것입니다.

